{
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build": "npm run build:clean && npm run build:copy",
    "build:clean": "rm -rf dist && mkdir -p dist",
    "build:copy": "cp index.html script.js style.css dist/"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "eslint": "^8.57.1",
    "eslint-config-google": "^0.14.0",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "jsdom": "^29.0.2"
  }
}
